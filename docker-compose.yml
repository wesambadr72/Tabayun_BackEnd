services:
  pgvector:
    image: pgvector/pgvector:pg16
    container_name: tabayun-pgvector
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Wesam123456
      POSTGRES_DB: tabayun_db
    ports:
      - "5434:5432"
    volumes:
      - pgvector_data:/var/lib/postgresql/data

volumes:
  pgvector_data:
